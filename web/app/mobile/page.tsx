import MobileDemo from "@/components/MobileDemo";

export const metadata = {
  title: "VALSYNC — Interactive Phone Demo",
  description: "VALSYNC interactive phone demo — swipe through real app screenshots.",
};

export default function MobilePage() {
  return <MobileDemo />;
}
